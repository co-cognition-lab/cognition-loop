# 新项目说明：Lab 公共认知回路

> 创建日期：2026-06-01
> 创建人：strategic(pure thinking) 起草
> 定位：Co-Cognition Lab 的公共入口——让外部人以最低门槛参与 ITEC 认知协作

---

## 为什么需要这个项目

P5（ITEC 认知机制）是学术研究管线——框架、论文、实验设计。
P5 的论文出口是 cs.CL/cs.AI。

但这个论文是单向的：我们写 → 学术界读。缺少一个**双向回路**——外部人不仅能读到我们的工作，还能往里面放东西。

ITEC 的特殊性在于：它不是我们发明的罕见现象。**每个人用 LLM 都经历过——只是没人叫出它的名字。** 这意味着外部人的"我也遇到过！"本身就是数据。这个数据如果只存在于我们的内部文件中，永远不会变成验证方法论正确性的独立证据。

**这个新项目做的事：** 建一个低门槛的入口，让任何人——不需要读论文、不需要理解框架——就能提交他们遇到的 ITEC 事件。每一个提交都是在验证或挑战 3×4 矩阵。提交者不需要知道分类学，他们的原始描述被 P5 的方法论分析后归类，他们看到分类结果后，自然被拉进认知协作。

---

## 项目名称（候选）

- `cognition-loop`（✅ 已选用）
- `itec-registry`
- `co-cognition-commons`

已选用：`cognition-loop`——简洁，public 属性由 GitHub 可见性表达。

---

## 核心机制

### 入口：ITEC 事件提交模板

不需要注册、不需要登录。一个极简表单：

```
1. 你用的 LLM 是什么（产品+模型）？
2. 你让它做什么（一句话描述任务）？
3. 它实际做了什么（和你的期望偏差在哪里）？
4. 你觉得问题出在哪里（可选，你自己的诊断）？
5. 如果愿意，留联系方式（可选，用于我们回复分析结果）
```

提交后：
- **即时**：事件进入公共注册表（匿名，仅显示 #编号）
- **24-48h 内**：P5 方法论引擎（三槽位分析）返回分类结果
- **公开**：分类结果 + 原始提交一起展示，形成 ITEC 案例库

### 回路：提交 → 分析 → 反馈 → 再参与

```
外部人提交 ITEC 事件
    │
    ▼
公共注册表（所有人可见）
    │
    ▼
三槽位方法论引擎自动分析
    │
    ├── 匹配到 3×4 矩阵格位 → 返回："你的事件属于 [执行层/跳过前提验证]，详见分析"
    │
    └── 无法匹配 → 返回："你的事件未匹配现有分类 → 可能是新格位候选 → 已加入研究队列"
    │
    ▼
提交者收到反馈 → 看到自己的经历被命名和理解 → 
    ├── 继续提交更多事件
    ├── 浏览其他人的事件（"我也是！"）
    ├── 开始理解 ITEC 框架
    └── 自然地进入深度参与（GitHub Star / Issue / 提供更多数据）
```

### 三阶段实施

| 阶段 | 内容 | 产出 |
|:--|:--|:--|
| **Phase 1：种子注册表** | GitHub Repo 创建 + 提交模板（Markdown Issue Template）+ 添加 P5 已有的 6 个实证事件作为种子数据 | 可浏览的公共 ITEC 事件库 |
| **Phase 2：手动分析回路** | 接收外部提交 → 手动三槽位分析 → 返回结果 → 更新注册表 | 验证"外部人提交→分析→反馈"回路是否成立 |
| **Phase 3：自动化** | OpenClaw Skill 实现自动三槽位分析 → co-cognition.org 集成提交表单 → 自动返回分类结果 | 零人工干预的公共认知协作回路 |
| **Phase 4：论文引用** | P5 论文引用公共注册表中的事件作为独立验证数据 | 学术产出 + 社区数据互相增强 |

---

## 与现有项目的关系

| 项目 | 关系 |
|:--|:--|
| **P5 ITEC** | 方法论引擎和分析框架的来源。P5 的 3×4 矩阵是这个项目的分析标准。P5 论文引用本项目的公共注册表作为独立验证数据源 |
| **P1 直觉理论** | ITEC 的理论基础（代价缺失 → 执行冲动）。本项目的公共事件可以做 P1 假说 B（退化风险）的外部实证锚点 |
| **website-content-manager** | 提交表单和注册表展示需要网站支持。Phase 3 的自动化回路部署在 co-cognition.org 上 |
| **Hook 实验** | 复合攻击向量的外部验证——外部人提交的 ITEC 事件可能与钩子触发模式重合 |
| **strategic-advisor** | 本项目由 strategic-advisor 追踪和协调。Kimi 邮件策略和对外联络在此管理 |

---

## 需要跨项目协调的事项

1. **GitHub Repo 创建**：在 co-cognition-lab 组织下新建 repo。Repo 结构包含 ISSUE_TEMPLATE（提交模板）、README（项目说明）、seed-events/（种子事件目录）
2. **网站集成**：co-cognition.org 新增 `/zh/itec` 页面——说明什么是 ITEC + 提交入口
3. **P5 对齐**：P5 的 blind review request 和外审策略需要知道公共注册表的存在——注册表的数据可以作为外审材料的一部分
4. **三槽位 Skill 开发**：Phase 3 的核心——一个 OpenClaw Skill 能自动接收 ITEC 事件文本 → 分发给三槽位 → 生成分类报告

---

## 成功判据

Phase 1 的判据：**种子注册表上线后，是否有第一个非 Lab 成员提交 ITEC 事件。** 不需要量大——一个来自陌生人的、真实的、我们没见过的 ITEC 变体，就是这个回路成立的信号。

后续判据：
- 外部提交事件中，≥60% 能匹配到 3×4 矩阵现有格位（验证分类框架的覆盖率）
- 外部提交事件中，至少 1 个无法匹配 → 触发格位扩展（证明回路产生了新知识）

---

*本文件待转移到新项目目录后，新项目的 agent 以此为 PROJECT_BRIEF，进一步细化实施细节。*
